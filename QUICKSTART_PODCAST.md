# 快速开始 - 播客功能

## 30秒快速开始

```bash
# 1. 设置 API Key
export OPENROUTER_API_KEY=""

# 2. 安装 TTS 依赖
pip install edge-tts

# 3. 运行爬虫
python main.py

# 4. 生成播客
python generate_podcast_auto.py

# 5. 重新生成 HTML
python main.py
```

打开生成的 `index.html` 或最新的HTML文件，你会在页面顶部看到播客播放器！

## 效果预览

生成的 HTML 页面顶部会出现这样的播放器：

```
┌────────────────────────────────────────────┐
│         热点新闻分析                         │
│  ┌──────────────────────────────────────┐  │
│  │ 🎧 播客音频                            │  │
│  │ ───○────────── 00:00 / 03:45  ⏯  │  │
│  └──────────────────────────────────────┘  │
│  报告类型: 当日汇总  新闻总数: 150 条       │
└────────────────────────────────────────────┘
```

## 文件说明

### 自动生成的文件

```
output/2025年11月25日/
├── txt/
│   └── 21时41分.txt             # 新闻原始数据
├── html/
│   └── 21时41分.html            # 带播放器的HTML
└── audio/
    ├── 21时41分.mp3             # 播客音频 ⭐
    └── 21时41分_script.txt      # AI生成的脚本
```

### 脚本文件

- `generate_podcast.py` - 交互式生成（本地开发用）
- `generate_podcast_auto.py` - 自动化生成（CI/CD用）
- `test_audio_integration.py` - 测试脚本

## 常见问题

### Q1: 没有看到播放器？

**A:** 确保音频文件存在且文件名匹配：
```bash
ls -lh output/$(date +%Y年%m月%d日)/audio/
```

如果文件不存在，运行：
```bash
python generate_podcast_auto.py
```

### Q2: 播放器显示但无法播放？

**A:** 音频文件可能是空的。重新生成音频：
```bash
export OPENROUTER_API_KEY="your-key"
python generate_podcast_auto.py
```

### Q3: AI 脚本生成失败？

**A:** 检查以下几点：
1. API Key 是否正确
2. 网络是否正常
3. 查看错误信息：
```bash
python generate_podcast_auto.py 2>&1 | grep "ERROR"
```

### Q4: 想要更改语音？

**A:** 编辑 `generate_podcast_auto.py` 第 221 行：
```python
# 女声温柔版
communicate = edge_tts.Communicate(script, "zh-CN-XiaoxiaoNeural")

# 男声沉稳版
communicate = edge_tts.Communicate(script, "zh-CN-YunxiNeural")

# 男声播报版（默认）
communicate = edge_tts.Communicate(script, "zh-CN-YunyangNeural")
```

## GitHub Actions 配置

在 `.github/workflows/update.yml` 中添加：

```yaml
      # 现有的爬虫步骤
      - name: Run crawler
        run: python main.py

      # 🎙️ 新增：生成播客
      - name: Generate podcast
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        run: |
          pip install edge-tts
          python generate_podcast_auto.py || echo "Podcast generation failed, continuing..."

      # 🎙️ 新增：重新生成带音频的HTML
      - name: Regenerate HTML
        run: python main.py

      # 提交更改
      - name: Commit changes
        run: |
          git add output/ index.html
          git commit -m "Auto update with podcast"
          git push
```

别忘了在 GitHub Secrets 中添加 `OPENROUTER_API_KEY`！

## 高级配置

### 自定义播客风格

编辑 `generate_podcast_auto.py`，修改 prompt：

```python
prompt = f"""你是一位【幽默风趣】的播客主播...  # 改成你想要的风格
要求：
1. 语言风格【轻松搞笑】                      # 自定义语言风格
2. 使用【网络流行语】                        # 添加特色要求
...
```

### 控制播客长度

调整 `max_tokens` 参数：

```python
"max_tokens": 2000,  # 增加 → 更长的播客
```

### 批量生成历史播客

```bash
# 为今天所有的新闻生成播客
for txt_file in output/$(date +%Y年%m月%d日)/txt/*.txt; do
    python generate_podcast_auto.py
    python main.py
done
```

## 费用说明

- OpenRouter (DeepSeek V3): ~$0.001/次
- Edge TTS: 完全免费
- 总计: **~$0.001/次**（约0.007元人民币）

每天运行12次，月费用约 **$0.36**（约2.5元人民币）

## 测试验证

运行测试脚本验证集成：

```bash
python test_audio_integration.py
```

应该看到：
```
🎉 所有测试通过！音频播放器集成成功！
```

## 下一步

- [ ] 自定义播客风格
- [ ] 配置 GitHub Actions
- [ ] 添加背景音乐
- [ ] 分享给朋友！

## 需要帮助？

查看完整文档：
- [README_PODCAST.md](./README_PODCAST.md) - 详细文档
- [INTEGRATION_SUMMARY.md](./INTEGRATION_SUMMARY.md) - 集成总结

## 享受你的播客新闻！🎉

现在每次生成新闻报告时，都会自动生成对应的播客音频，让你可以一边做事一边听新闻！
